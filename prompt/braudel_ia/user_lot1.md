Construis le lot 1 du pipeline IA Braudel.

Objectif :
- créer la structure initiale du projet `ia-braudel/` ;
- définir les taxonomies fermées ;
- créer les fichiers d’ontologie de base ;
- expliciter les tâches autorisées ;
- poser les constantes métier partagées.

Contraintes :
- aucune tâche hors liste autorisée ;
- aucune dépendance à un service distant ;
- ne pas implémenter encore le runtime complet ;
- ne pas écrire de schémas vagues ;
- garder une séparation claire entre ontology, schemas, inference, training et evaluation.

Livrables attendus :
- arborescence initiale ;
- `entity_types.json` ;
- `relation_types.json` ;
- `timeline_types.json` ;
- `style_types.json` ;
- `operation_types.json` ;
- constantes Python ou JSON réutilisables ;
- premiers tests de cohérence sur listes fermées.

Réponds strictement :
PLAN
FILES
CODE
CHECKS
NEXT